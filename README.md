# lightwire
An audio streaming engine for ultra low-latency

## Cloning this repository

If you want to clone this repository, you need to run this command after cloning it:

```sh
git submodule update --init --recursive
```

This makes sure you have all the dependencies pulled to your local version of the repository.
